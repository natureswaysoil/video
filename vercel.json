{
  "buildCommand": "echo 'Documentation already in public/' && ls -la public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
